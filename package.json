{
  "name": "aws-cross-account-iam-monorepo",
  "private": true,
  "workspaces": [
    "cli",
    "website"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=website",
    "test": "npm run test --workspace=cli"
  }
}
